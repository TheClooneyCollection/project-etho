services:
  sheet-pipeline:
    image: sheet-pipeline
    build: .
    env_file:
      - .env
    volumes:
      - ./pipeline.py:/app/pipeline.py:ro
      - ./video_enrich.py:/app/video_enrich.py:ro
      - ./out:/out
    # no command here — we pass it at runtime
