#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PIPELINE_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

mkdir -p "$PIPELINE_DIR/../data"
docker compose -f "$PIPELINE_DIR/compose.yml" run --rm sheet-pipeline python /app/pipeline.py
